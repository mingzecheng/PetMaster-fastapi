# 支付宝签名验证失败 - 快速解决方案

## 问题总结

您遇到的 `invalid-signature` 错误，经过分析，最可能的原因是：

**支付宝沙箱默认密钥的格式问题**

支付宝沙箱提供的"默认密钥"和我们使用的 `python-alipay-sdk` 可能存在兼容性问题。

---

## 最终解决方案

### 方案：使用自己生成的密钥对（推荐）

**步骤1：生成新的密钥对**

在您的电脑上运行（需要 openssl）：

```bash
# 步骤1：生成私钥（PKCS8格式）
openssl genrsa -out app_private_key_2048.pem 2048

# 步骤2：从私钥提取公钥
openssl rsa -in app_private_key_2048.pem -pubout -out app_public_key_2048.pem

# 步骤3：将私钥转换为PKCS8格式（python-alipay-sdk推荐）
openssl pkcs8 -topk8 -inform PEM -in app_private_key_2048.pem -outform PEM -nocrypt -out app_private_key_pkcs8.pem
```

或者使用支付宝官方工具：https://opendocs.alipay.com/common/02kipl

**步骤2：上传公钥到支付宝沙箱**

1. 打开 `app_public_key_2048.pem` 文件
2. 复制 **整个内容**（包括 BEGIN/END 行）或只复制中间的密钥字符串
3. 登录支付宝沙箱：https://openhome.alipay.com/develop/sandbox/app
4. 进入【接口加签方式】
5. 选择【公钥】模式
6. 粘贴公钥内容并保存
7. 保存后会显示 **支付宝公钥**，复制它

**步骤3：配置到 .env**

```bash
# 支付宝配置
ALIPAY_APP_ID=9021000157683106
ALIPAY_USE_SANDBOX=true

# 使用您刚生成的PKCS8私钥（整个文件内容，包括BEGIN/END）
ALIPAY_APP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
MIIEvQIBADANBgkqhkiG9w0... （您的私钥完整内容）
...
-----END PRIVATE KEY-----"

# 使用支付宝返回的支付宝公钥（整个内容或纯字符串都可以）
ALIPAY_ALI_PUBLIC_KEY="MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA... （支付宝公钥）"

# 回调地址
ALIPAY_NOTIFY_URL=https://audrina-featherlight-jerrold.ngrok-free.dev/api/payments/alipay/notify
ALIPAY_RETURN_URL=http://127.0.0.1:5173/pages/payment/pay
```

**步骤4：重启后端**

重启 FastAPI 服务，查看启动日志：

应该看到：
```
[诊断] 检测到 PKCS8 格式私钥
[诊断] 支付宝客户端初始化成功
```

**步骤5：测试支付**

重新测试商品购买或会员卡充值。

---

## 为什么沙箱默认密钥可能有问题？

1. **格式不兼容**：沙箱默认密钥可能是 PKCS1 格式，而 `python-alipay-sdk` 某些版本推荐 PKCS8
2. **密钥对不匹配**：如果沙箱显示的"应用私钥"和"支付宝公钥"不是从同一对密钥生成，会导致签名验证失败
3. **编码问题**：复制粘贴时可能引入隐藏字符

---

## 验证密钥是否正确

生成密钥后，可以验证公钥和私钥是否匹配：

```bash
# 从私钥提取公钥
openssl rsa -in app_private_key_pkcs8.pem -pubout

# 对比和您上传到支付宝的公钥是否一致
```

两个公钥应该完全相同。

---

## 仍然失败？

如果按照以上步骤操作后仍然失败，请：

1. **查看后端启动日志**，确认：
   - 密钥是否正确加载
   - SDK 是否初始化成功
   - 密钥格式是否被正确识别

2. **检查支付宝沙箱应用配置**：
   - 确认使用的是 **公钥模式**（不是证书模式）
   - 确认 **签名算法** 是 **RSA2**

3. **联系我**：
   - 提供后端启动日志（前50行）
   - 提供支付宝沙箱的应用配置截图

---

## 快捷方案（临时）

如果您现在急需测试其他功能，可以先跳过支付宝验证：

在 `payment_service.py` 中添加一个测试模式，创建假支付（仅用于开发测试）。

但**强烈建议**还是解决签名问题，因为这是支付功能的核心。
