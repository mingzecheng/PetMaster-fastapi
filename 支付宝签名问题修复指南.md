# 支付宝签名验证错误 (invalid-signature) 修复指南

## 问题分析

您遇到的错误：
```
错误代码: invalid-signature
错误原因: 验签出错，建议检查签名字符串或签名私钥与应用公钥是否匹配
```

从错误信息中发现的问题：
```
notify_url=https://audrina-featherlight-jerrold.ngrok-free.dev
```

**问题1：notify_url 不完整**
当前的 notify_url 只有域名，缺少API路径。应该是：
`https://audrina-featherlight-jerrold.ngrok-free.dev/api/payments/alipay/notify`

**问题2：签名验证失败的常见原因**
支付宝签名验证失败（invalid-signature）通常由以下原因造成：
1. **应用私钥和应用公钥不匹配**（最常见）
2. 密钥格式错误
3. 使用了错误的签名类型

---

## 解决方案

### 步骤1：修复 notify_url 配置

在 `.env` 文件中设置完整的 notify_url：

```bash
# 支付宝配置
ALIPAY_APP_ID=你的应用ID
ALIPAY_USE_SANDBOX=true

# ⚠️ 重要：加上完整的API路径
ALIPAY_NOTIFY_URL=https://audrina-featherlight-jerrold.ngrok-free.dev/api/payments/alipay/notify
ALIPAY_RETURN_URL=http://127.0.0.1:5173/pages/payment/pay

# 密钥配置（见步骤2）
ALIPAY_APP_PRIVATE_KEY=你的应用私钥
ALIPAY_ALI_PUBLIC_KEY=支付宝公钥
```

---

### 步骤2：检查并重新配置密钥（关键）

#### 2.1 生成新的应用密钥对

使用支付宝官方工具生成密钥对：
https://opendocs.alipay.com/common/02kipl

或使用命令行（推荐PKCS8格式）：
```bash
# 生成私钥
openssl genrsa -out app_private_key.pem 2048

# 从私钥提取公钥
openssl rsa -in app_private_key.pem -pubout -out app_public_key.pem
```

#### 2.2 上传应用公钥到支付宝

1. 登录支付宝开放平台：https://open.alipay.com
2. 进入【开发信息】→【接口加签方式(密钥/证书)】
3. 选择【公钥】模式
4. 上传 `app_public_key.pem` 的内容（**只要中间的密钥字符串，不要BEGIN/END行**）
5. 保存后，支付宝会显示**支付宝公钥**，复制它

#### 2.3 配置到 .env 文件

```bash
# 应用私钥（app_private_key.pem的内容，可以包含BEGIN/END行，也可以只要中间字符串）
ALIPAY_APP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...
（你的完整私钥内容）
...
-----END PRIVATE KEY-----"

# 支付宝公钥（从支付宝开放平台复制，可以包含BEGIN/END行，也可以只要中间字符串）
ALIPAY_ALI_PUBLIC_KEY="MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA..."
```

**⚠️ 重要提示**：
- `ALIPAY_APP_PRIVATE_KEY` 是**你本地生成的应用私钥**
- `ALIPAY_ALI_PUBLIC_KEY` 是**支付宝平台提供的支付宝公钥**（不是你的应用公钥）
- 这两个必须配对使用！

---

### 步骤3：验证密钥格式

代码已经支持多种密钥格式：
- ✅ PKCS1 格式（BEGIN RSA PRIVATE KEY）
- ✅ PKCS8 格式（BEGIN PRIVATE KEY）
- ✅ 纯密钥字符串（无BEGIN/END行）

你可以使用任何一种格式，代码会自动转换。

---

### 步骤4：常见错误排查

#### 错误1：使用了应用公钥替代支付宝公钥
```bash
# ❌ 错误
ALIPAY_ALI_PUBLIC_KEY="你的应用公钥"  # 这是错误的！

# ✅ 正确
ALIPAY_ALI_PUBLIC_KEY="支付宝平台提供的支付宝公钥"
```

#### 错误2：私钥和公钥不是一对
确保：
1. 应用私钥 `ALIPAY_APP_PRIVATE_KEY`
2. 从应用私钥提取的应用公钥 → 上传到支付宝
3. 支付宝返回的支付宝公钥 → 配置到 `ALIPAY_ALI_PUBLIC_KEY`

#### 错误3：密钥中包含特殊字符或换行问题
在 `.env` 文件中：
```bash
# 方法1：使用引号包裹（推荐）
ALIPAY_APP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
MIIEvQIB...
-----END PRIVATE KEY-----"

# 方法2：只用单行字符串（无空格无换行）
ALIPAY_APP_PRIVATE_KEY=MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...
```

---

### 步骤5：重启服务并测试

1. 修改 `.env` 后重启 FastAPI 服务
2. 查看启动日志，确认密钥格式检测正确：
   ```
   [诊断] 检测到 PKCS8 格式私钥
   [诊断] 支付宝客户端初始化成功
   ```
3. 测试支付功能

---

## 快速检查清单

- [ ] `.env` 中 `ALIPAY_NOTIFY_URL` 包含完整API路径
- [ ] 使用的是**一对新生成的密钥**（私钥+公钥）
- [ ] 应用公钥已上传到支付宝开放平台
- [ ] `ALIPAY_ALI_PUBLIC_KEY` 配置的是**支付宝公钥**（不是应用公钥）
- [ ] 私钥和公钥是配对的
- [ ] 密钥格式正确（有BEGIN/END或纯字符串都可以）
- [ ] 重启了后端服务

---

## 仍然失败？

如果按照以上步骤操作后仍然失败，请检查：

1. **查看后端日志**
   启动时应该看到：
   ```
   [诊断] 原始私钥前20字符: MIIEvQIBADANBgkqhki...
   [诊断] 原始公钥前20字符: MIIBIjANBgkqhkiG9w...
   [诊断] 检测到 PKCS8 格式私钥
   [诊断] 支付宝客户端初始化成功
   ```

2. **验证密钥对是否匹配**
   ```bash
   # 从私钥提取公钥
   openssl rsa -in app_private_key.pem -pubout
   
   # 和你上传到支付宝的公钥对比，应该完全一致
   ```

3. **联系支持**
   提供错误日志和密钥生成方式，我可以进一步协助。

---

## 小提示

- 沙箱环境和生产环境的密钥是独立的
- 修改密钥后一定要重启后端服务
- ngrok的域名会变化，记得同步更新 `ALIPAY_NOTIFY_URL`
